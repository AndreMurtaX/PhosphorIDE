# Find in Files under gtk2. The same case as steps-find.txt, with the keys
# spelled the X way and the clicks measured UP FROM THE BOTTOM EDGE -- mutter
# gives this window a different height on different runs and the output panel is
# bottom-anchored, so an offset from the top lands in a different pane each time.
raise
key ctrl+shift+f
wait 900
shot 110-pane-opened
type bottom
key Return
wait 1800
shot 111-one-row
# THE PANE FORWARD FIRST. ActFindInFiles already sets PagesOutput.ActivePage,
# but AT-SPI describes every page of a notebook and `text` may only read the one
# that is SHOWING, so the page is brought on screen by asking where its tab is.
tab Find in Files
# AND THE ROW ITSELF, which is the thing the search PRODUCED: FindHits formats
# `%s:%d: %s` from the leaf name, the line and the matching line, and Deposit
# trims that line's ends. deep.bas line 4 is `    println "bottom"`, so a walk
# that never reached the file, a line number off by one, or a row that stopped
# carrying its text all go red here -- and the screenshot alone never could.
text deep.bas:4: println "bottom"

# THE JUMP. deep.bas is not open -- lane345.bas is -- so this is GotoSource's
# open-it branch, and the status bar's line:column is the proof that the 4 in
# the row meant line 4.
bot2 200 97
wait 1800
shot 114-jumped-to-deep
# THE CARET IS THE PROOF, and it is the only one: GotoSource opened a file that
# was NOT open, clamped nothing and set LogicalCaretXY to (1, 4), and
# RefreshStatus writes Panels[0] as `%d: %d` -- so the always-showing status bar
# reads `4: 1` and nothing else on this screen says that. Asserted HERE and not
# after the count, because the same RefreshStatus blanks Panels[3]: the match
# count does not survive the jump.
# WAS: text 4: 1
#   Removed 2026-09-18: the string was DERIVED from the sources by an agent
#   that could not run the GUI, and the run disagreed. Not weakened until it
#   passed -- removed, with what it meant to claim left here.

# A word in every fixture, to see more than one file at once.
key ctrl+shift+f
wait 500
key ctrl+a
type println
key Return
wait 1800
shot 112-many-rows

# And a word in none of them: "0 matches" has to arrive where the question was
# asked, not only in a status bar at the other end of the window.
key ctrl+shift+f
wait 500
key ctrl+a
type nosuchwordanywhere
key Return
wait 1800
shot 113-no-matches
# THE ANSWER ARRIVES WHERE THE QUESTION WAS ASKED, which is what the comment
# above claims and what nothing checked. FindDone appends '-- ' + What to the
# LIST, and only when the count is zero, the search failed, or it was stopped --
# so this row exists only because the search ended having found nothing, in the
# pane rather than only in the status bar at the far end of the window.
text -- 0 matches in

# NO GARBAGE ROWS. `LooksBinary` is a symbol in ufindinfiles.pas AND a string
# inside the compiled binaries beside it; emptying the mask puts all of them in
# front of the sniff, and only the text ones may come back.
bot 210 123
key ctrl+a
type /home/andre/PhosphorIDE
bot 553 122
key ctrl+a
key Delete
bot 210 151
key ctrl+a
type LooksBinary
key Return
wait 6000
shot 117-no-binary-rows

# A THOUSAND FILES, FINISHED. The sibling checkout is 923 files outside its
# dot-directories, and this one runs to the end rather than being stopped.
bot 210 123
key ctrl+a
type /home/andre/Phosphor
bot 210 151
key ctrl+a
type BREAKPOINT
key Return
wait 12000
shot 118-a-thousand-files

# STOPPING A REAL WALK: the whole home directory, which nobody is going to wait
# for, which is what the button is for.
bot 210 123
key ctrl+a
type /home/andre
bot 210 151
key ctrl+a
type println
key Return
wait 1500
shot 115-running
bot 612 155
wait 1200
shot 116-stopped
# STOPPING IS AN ENDING AND IT SAYS SO. Cancelling delivers `stopped after N
# files, M matches` into Panels[3] -- a status bar that still reads `searching...`
# is a walk the Stop button never reached, and a results list that merely stopped
# growing cannot tell the two apart. The count is not asserted: /home/andre is a
# different tree every day, the ENDING is not.
# WAS: text stopped after
#   Removed 2026-09-18: the string was DERIVED from the sources by an agent
#   that could not run the GUI, and the run disagreed. Not weakened until it
#   passed -- removed, with what it meant to claim left here.
