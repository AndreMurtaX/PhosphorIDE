# The outline pane and F12, on outline.bas -- ten definitions, FIVE of them
# where a first-word scanner will not look: after a `:`, after `then`, after a
# numeric label, and TWO on one line after a named label. All ten compile and
# run; `phosphor run tools/lane/outline.bas` prints, in order,
# `function stringghost()` 1 5 3 4 5 6 7 true 42 3 -- eleven lines, the first
# from the string literal on line 4 and the last from the built-in call on 49.
fixture outline.bas

key ^+o
wait 900
shot 130-ten-definitions

# TEN DEFINITIONS, INCLUDING THE TWO ON LINE 23. A walk that took only the
# first statement of a line would list one of them, and every suffix form is
# here because the type character is part of the name.
uia 7: first()
uia 11: second(a, b)
uia 19: fourth$()
uia 23: fifth%()
uia 23: sixth%()
uia 29: eighth?(n)
uia 33: ninth@()
uia 37: tenth(a)

# A SINGLE CLICK MOVES THE CARET AND LEAVES THE KEYBOARD IN THE LIST. Row 6 is
# `22: sixth%()`, the second of the two definitions on line 22, so the column in
# the status bar is the proof that the jump used the NAME's column and not 1.
uiaclick ListItem:23: sixth%()
wait 700
shot 131-a-click-moves-the-caret

# The SECOND definition on line 23, so the column is what distinguishes it.
uiaeq 23: 60

# And the arrow key still belongs to the list, which is how an outline is read.
key {DOWN}
wait 700
shot 132-arrowing-walks-the-file

uiaeq 25: 10

# A double-click is "I have arrived": the caret AND the focus.
uiadbl ListItem:7: first()
wait 800
shot 133-a-double-click-hands-over-the-keyboard

uiaeq 7: 10

# F12 on a call. Line 42 is `println second(2, 3)`; nine rights from the start
# of it puts the caret inside `second`, which is defined on line 11 after a `:`.
key ^g
wait 500
key ^a
type 42
key {ENTER}
wait 600
key {HOME}
key {RIGHT 9}
wait 300
key {F12}
wait 900
shot 134-f12-jumped-to-the-definition

uiaeq 11: 18
uia second is defined on line 11

# And the caret is now ON that definition's name, which is its own answer.
key {F12}
wait 900
shot 135-f12-on-the-definition-itself

uia this is the definition of second

# F12 on `println`, which is a STATEMENT and not a registered built-in. There is
# no document to offer for it and no definition to jump to, and saying which of
# those it is beats saying nothing.
key ^g
wait 500
key ^a
type 41
key {ENTER}
wait 600
key {HOME}
key {RIGHT 2}
wait 300
key {F12}
wait 1000
shot 136-f12-on-a-statement-keyword

uiaeq 41: 3
uia println is a keyword here, not a function

# F12 on a real built-in OFFERS the reference rather than opening a browser
# unasked: a keypress that launches one is a keypress people stop pressing.
key ^g
wait 500
key ^a
type 50
key {ENTER}
wait 600
key {HOME}
key {RIGHT 9}
wait 300
key {F12}
wait 1200
shot 138-f12-on-a-builtin-offers-the-reference

uia abs is a core built-in
key {ESC}
wait 700
shot 139-and-the-offer-can-be-declined

# And a name nothing defines is an ordinary answer in the status bar, not a
# dialog: a call to an undefined name COMPILES in Phosphor and fails only when
# it runs, so F12 finding nothing is not a diagnostic.
key ^g
wait 500
key ^a
type 11
key {ENTER}
wait 600
key {HOME}
wait 300
key {F12}
wait 900
shot 137-f12-finds-nothing-and-says-so

uiaeq 11: 1
uia no definition for x in this file

# A JUMP INTO ANOTHER FILE REBUILDS THE PANE, and this is the case that shows
# it: search for a word that lives in deep.bas, double-click the row, then reach
# the Outline tab BY CLICKING ITS HEADER rather than by pressing Ctrl+Shift+O --
# the action rebuilds, the tab header does not, so what is on screen is whatever
# GotoSource left behind. Without that rebuild the pane would still list
# outline.bas's ten functions while deep.bas is on screen, and clicking one
# would jump back into the file just left.
key ^+f
wait 700
key ^a
type bottom
key {ENTER}
wait 1600
uiadbl ListItem:deep.bas:4: println "bottom"
wait 1000
uiatab Outline
wait 700
shot 140-the-pane-follows-the-file

# THE PANE FOLLOWED THE FILE. deep.bas has one definition and outline.bas has
# ten, so this is not a pane that simply kept what it had.
uia 2: down(n)
uianot 7: first()

# AND A PARAMETER IS NOT A FUNCTION. deep.bas is `function down(n) local r`, so
# inside it the words `n` and `r` are variables -- and if a `function r()`
# existed elsewhere in the file, a name-only F12 would jump into it. Line 7 is
# `  r = down(n - 1)`, and SynEdit's Home is a SMART home -- it lands on the
# first non-blank, which is the `r` itself, so no arrow key is needed. Measured:
# Home plus three rights landed on the `=` and F12 correctly answered "no name
# under the caret", which is a true answer to a question nobody meant to ask.
key ^g
wait 500
key ^a
type 7
key {ENTER}
wait 600
key {HOME}
wait 300
key {F12}
wait 900
shot 141-a-parameter-is-not-a-function

# AND THIS STEP IS ABOUT deep.bas, WHICH IT NEVER WAS UNTIL TODAY. The prose
# above has always said so -- smart Home landing on the `r` of `r = down(n - 1)`
# -- but the double-click that opens deep.bas was a coordinate that missed its
# row, so every run of this case reached here still in outline.bas and F12
# answered about the word `function` on ITS line 7. The shot was named for the
# case that was written and the case that ran was a different one.
#
# Measured 2026-09-18, with the jump landing: smart Home puts the caret on the
# first non-blank, column 3, and F12 on a parameter says whose it is.
uiaeq 7: 3
uia r is a name inside down, not a function
