# A gutter mark has to follow its statement, and this is the case nobody calls:
# typing a line ABOVE a breakpoint moves the breakpoint, and the mark is a second
# copy of that fact drawn by SynEdit. Set one on line 10, insert a line at the
# top, and the mark must be on 11 -- not left behind on 10.
fixture lane345.bas

key ^g
wait 700
type 10
key {ENTER}
wait 500
key {F5}
wait 600
shot 61-mark-on-10

uiaeq 10: 1
key ^g
wait 700
type 2
key {ENTER}
wait 500
key {HOME}
wait 200
key {ENTER}
wait 800
shot 62-mark-followed-to-11

# The caret is on 3 because a line was inserted at 2; the mark that was on 10
# is now on 11, and that half is the shot.
uiaeq 3: 1
uia modified
unassertable the mark is in the gutter and there is no reading of it here. The arithmetic under it is pinned without a window by phosphoridetest, over every boundary case, and TBreakpointSet.TrackEdit is where it lives; this case is the wiring between the two, which only a picture shows
