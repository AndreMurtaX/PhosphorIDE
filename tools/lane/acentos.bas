rem Duas linhas identicas menos pelos acentos. A de baixo tem 17 caracteres e
rem 19 bytes, entao o cursor no fim dela e a coluna 18 por uma medida do SynEdit
rem e 20 pela outra -- e LineText e medido em bytes.
s$ = "acao" + pri
t$ = "ação" + pri
