rem Two lines identical but for the accents. The lower one is 17 characters and
rem 19 bytes, so the caret at its end is column 18 by one SynEdit measure and
rem 20 by the other -- and LineText is measured in bytes.
s$ = "acao" + pri
t$ = "ação" + pri
