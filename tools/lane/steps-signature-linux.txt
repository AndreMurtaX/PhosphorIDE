# Signature help under gtk2. Same case as steps-signature.txt; every character
# goes in by keysym name, so no layout gets a vote and no dead key composes
# with the vowel after it.
raise
key ctrl+End
wait 400
key Return
wait 300
type s = mid$(
wait 900
shot 111-first-argument
# WHAT THE HINT SAYS, not merely that a window appeared. `mid$` is registered
# TWICE -- `$n` and `$nn`, the two rows `SignatureCodes` carries for it -- and
# the three-argument arity exists in this editor only because gen-keywords.py
# keeps every slot instead of the last one (roadmap item 12). The brackets are
# where the caret is: argument 0, which is what `mid$(` with nothing after it
# means.
type "z",
wait 900
shot 112-second-argument
# AND THE HINT ITSELF. A GTK popup is an override-redirect window of its own, so
# the shot above has the text and not the window over it; popshot finds the
# window that was not there at startup.
popshot 112b-the-hint-itself
# THE MARK MOVED WITH THE COMMA. Same rows, one argument along: the kinds are
# unchanged and only the brackets travelled, which is the whole of what
# AArgIndex is for (uphosphorcomplete.pas:137-140). A hint that appeared once
# and then froze -- the FSigShown debounce refusing to re-activate, or
# EditorStatusChange no longer calling RefreshSignatureHint -- passes the line
# above and fails this one.
type 2, 3
wait 900
shot 113-third-argument
# BOTH ARITIES, PROVED SEPARATELY -- and this is the ONLY point in the case
# where they can be. At arguments 0 and 1 the short rendering is a PREFIX of the
# long one, so a substring match cannot tell two rows from one. At argument 2 it
# can: `$n` has no third argument to mark, so it renders unmarked and ends at
# `number)`, while `$nn` ends at `[number])`. Neither string contains the other.
# And the arity that no longer matches what is being typed is still LISTED,
# unmarked rather than hidden, which is the rule uphosphorcomplete.pas:138-140
# states and which nothing has ever checked: "saying so by marking nothing is
# quieter than hiding the row".
type )
wait 900
shot 114-closed-and-gone
# THE CLOSING PARENTHESIS WENT IN. Not decoration: the Windows twin of this case
# wrote `key )`, SendKeys swallowed it, the hint stayed up and it read as a
# dismissal bug -- steps-signature.txt:23-26 records the run it cost. Nothing
# here can assert an ABSENCE, so this asserts the input to it instead, off the
# status bar's caret panel, which is a number the editor computed.
#
# Line 15 because lane345.bas is 14 lines (utextfile.SplitLines gives a trailing
# newline no final empty line), ctrl+End lands on line 14 and Return opens 15.
# Column 19 is one past the 18 characters of `s = mid$("z",2, 3)`; eighteen means
# the `)` was dropped and the shot above is of a hint that should have been gone.
# DROPPED by the 2026-09-18 critique: text 15: 19
#   It is green with the feature deleted. See tools/lane/README.md.
key Return
wait 300
type t = callfunc(
wait 900
shot 115-nothing-known

# THE HINT ITSELF CANNOT BE READ ON THIS PLATFORM, and that is measured rather
# than assumed. FSigHint is a THintWindow (src/umainform.pas:424); gail does not
# describe it. Measured 2026-09-18 by typing `s = mid$(` into this very fixture
# and dumping every SHOWING control: no tool tip, no label, and nothing anywhere
# carrying `mid$`, `string` or `number`. All four needles this case was given
# failed for that one reason, and weakening them until they passed would have
# made the case say something it cannot know.
#
# The Windows twin READS the hint -- WM_GETTEXT crosses to a THintWindow there --
# so the feature is checked, on the platform that can check it.
unassertable the signature hint is a THintWindow and gail does not describe it. The Windows twin was cited here as the place it IS checked, and on 2026-09-18 that was measured: UIA does describe the window -- `mid$([string], number) | mid$([string], number, number)`, both arities -- but the hint itself appeared in one run of twelve there. Typed by hand it appears every time -- measured 2026-09-18 -- so what neither driver can reach is the hint, not the feature, and both sides declare it rather than gating on it
